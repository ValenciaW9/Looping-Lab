const gifts = [ "teddy bear", "drone","doll"];

function wrapGifts(gifts) {
    let i =0; // the intitialization mooves OUTSIDE the body of the loop!
    while)i <gifts.length) {
    console.log(~Wrapped ${gifts[1]} and added a bow!`);
    i ++; // the iteration moves INSIDE the body of the loop!

    }

    return gifts ;

}

weapGifts(gifts);
// LOG Wrapped teddy bear and added a bow!
// LOG: Wrapped drone and added a bow!
// LOG: Wrapped doll and added a bow!
// => ["teddy bear", "drone", "doll"]



function plantGardern()  {
    let keepWorking = true;
    while(keepWorking) {
        chooseSeedLocation():
        plantSeed();
        waterSeed();
        keepWorking = checkForMoreSeeds();
    }
}
 
//
let countup =0;
while (countup <10) {
    console.log(countup ++);
}

//
for(let countup =0; countup< 10; countup++)  {
    console.log(countup);
}

///




